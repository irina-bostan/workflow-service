<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.3</version>
    <relativePath/>
  </parent>

  <groupId>com.aniri</groupId>
  <artifactId>workflow-service</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <properties>
    <java.version>25</java.version>
    <maven.compiler.source>25</maven.compiler.source>
    <maven.compiler.target>25</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <springdoc.version>2.7.0</springdoc.version>
    <mapstruct.version>1.6.3</mapstruct.version>
    <lombok.version>1.18.38</lombok.version>
    <restassured.version>5.5.0</restassured.version>
    <awssdk.version>2.29.0</awssdk.version>
    <openapi-generator.version>7.21.0</openapi-generator.version>
    <cucumber.version>7.20.1</cucumber.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.cucumber</groupId>
        <artifactId>cucumber-bom</artifactId>
        <version>${cucumber.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>

    <!-- ==================== SPRING BOOT CORE ==================== -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-cache</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-aop</artifactId>
    </dependency>

    <!-- ==================== SECURITY ==================== -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
    </dependency>

    <!-- ==================== API DOCUMENTATION ==================== -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <!-- ==================== DATABASES ==================== -->
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>

    <!-- ==================== OBSERVABILITY ==================== -->
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-tracing-bridge-otel</artifactId>
    </dependency>

    <dependency>
      <groupId>io.opentelemetry</groupId>
      <artifactId>opentelemetry-exporter-otlp</artifactId>
    </dependency>

    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>

    <dependency>
      <groupId>net.logstash.logback</groupId>
      <artifactId>logstash-logback-encoder</artifactId>
      <version>8.0</version>
    </dependency>

    <!-- Enables <if> in logback-spring.xml so a property toggles JSON vs text appender -->
    <dependency>
      <groupId>org.codehaus.janino</groupId>
      <artifactId>janino</artifactId>
      <version>3.1.12</version>
    </dependency>

    <!-- ==================== AWS SDK v2 ==================== -->
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>sqs</artifactId>
      <version>${awssdk.version}</version>
    </dependency>

    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>sts</artifactId>
      <version>${awssdk.version}</version>
    </dependency>

    <!-- ==================== CODE GENERATION ==================== -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <version>${lombok.version}</version>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.mapstruct</groupId>
      <artifactId>mapstruct</artifactId>
      <version>${mapstruct.version}</version>
    </dependency>

    <!-- ==================== TESTING ==================== -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>io.rest-assured</groupId>
      <artifactId>rest-assured</artifactId>
      <version>${restassured.version}</version>
      <scope>test</scope>
    </dependency>

    <!-- ==================== BDD (Cucumber + JUnit 5 platform) ==================== -->
    <dependency>
      <groupId>io.cucumber</groupId>
      <artifactId>cucumber-java</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>io.cucumber</groupId>
      <artifactId>cucumber-spring</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>io.cucumber</groupId>
      <artifactId>cucumber-junit-platform-engine</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.junit.platform</groupId>
      <artifactId>junit-platform-suite</artifactId>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>

      <!-- Lombok must be processed before MapStruct -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <source>${java.version}</source>
          <target>${java.version}</target>
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
              <version>${lombok.version}</version>
            </path>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok-mapstruct-binding</artifactId>
              <version>0.2.0</version>
            </path>
            <path>
              <groupId>org.mapstruct</groupId>
              <artifactId>mapstruct-processor</artifactId>
              <version>${mapstruct.version}</version>
            </path>
          </annotationProcessorPaths>
          <compilerArgs>
            <arg>-Amapstruct.defaultComponentModel=spring</arg>
          </compilerArgs>
        </configuration>
      </plugin>

      <!-- Generate API models and controller interfaces from OpenAPI spec (API-first) -->
      <plugin>
        <groupId>org.openapitools</groupId>
        <artifactId>openapi-generator-maven-plugin</artifactId>
        <version>${openapi-generator.version}</version>
        <executions>
          <execution>
            <goals>
              <goal>generate</goal>
            </goals>
            <configuration>
              <inputSpec>${project.basedir}/src/main/resources/static/openapi.yaml</inputSpec>
              <generatorName>spring</generatorName>
              <apiPackage>com.aniri.workflow_service.web.api</apiPackage>
              <modelPackage>com.aniri.workflow_service.web.model</modelPackage>
              <generateApis>true</generateApis>
              <generateModels>true</generateModels>
              <generateApiTests>false</generateApiTests>
              <generateModelTests>false</generateModelTests>
              <generateSupportingFiles>true</generateSupportingFiles>
              <globalProperties>
                <supportingFiles>ApiUtil.java</supportingFiles>
              </globalProperties>
              <typeMappings>
                <typeMapping>UUID=java.util.UUID</typeMapping>
              </typeMappings>
              <configOptions>
                <useSpringBoot3>true</useSpringBoot3>
                <useJakartaEe>true</useJakartaEe>
                <delegatePattern>true</delegatePattern>
                <openApiNullable>false</openApiNullable>
                <useBeanValidation>true</useBeanValidation>
                <documentationProvider>springdoc</documentationProvider>
                <annotationLibrary>swagger2</annotationLibrary>
                <interfaceOnly>false</interfaceOnly>
                <skipDefaultInterface>false</skipDefaultInterface>
                <hideGenerationTimestamp>true</hideGenerationTimestamp>
                <serializationLibrary>jackson</serializationLibrary>
              </configOptions>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Unit + slice tests only (ITs are excluded — they run on `mvn verify`) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>**/*IT.java</exclude>
            <exclude>**/*IntegrationTest.java</exclude>
          </excludes>
          <!-- Cucumber engine auto-discovers .feature files; point it at nothing during `mvn test`
               so BDD scenarios only execute on `mvn verify`. -->
          <systemPropertyVariables>
            <cucumber.features>classpath:no-such-features</cucumber.features>
          </systemPropertyVariables>
        </configuration>
      </plugin>

      <!-- Integration tests (H2 PostgreSQL mode, full Spring context) run during `mvn verify` -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <configuration>
          <includes>
            <include>**/*IT.java</include>
            <include>**/*IntegrationTest.java</include>
          </includes>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>

  <profiles>
    <profile>
      <id>dev</id>
      <properties>
        <spring.profiles.active>dev</spring.profiles.active>
      </properties>
    </profile>
    <profile>
      <id>local</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <properties>
        <spring.profiles.active>local</spring.profiles.active>
      </properties>
    </profile>
    <profile>
      <id>stage</id>
      <properties>
        <spring.profiles.active>stage</spring.profiles.active>
      </properties>
    </profile>
    <profile>
      <id>prod</id>
      <properties>
        <spring.profiles.active>prod</spring.profiles.active>
      </properties>
    </profile>

    <!--
      Performance profile. Drives JMeter from Maven against a *running* app
      (assumes `bash local/run-local.sh` + `mvn spring-boot:run -Plocal` are up).
      Run with:
          mvn -Pperf verify -DskipTests -DskipITs
      Override scenario knobs via -D, e.g.:
          mvn -Pperf verify -DskipTests -DskipITs -Dperf.peak.duration=120 -Dperf.host=localhost
      Reports land in target/jmeter/reports.
    -->
    <profile>
      <id>perf</id>
      <properties>
        <perf.host>localhost</perf.host>
        <perf.port>8080</perf.port>
        <perf.protocol>http</perf.protocol>
        <perf.employeeId>EMP-PERF</perf.employeeId>

        <!-- Throughput is samples-per-MINUTE (JMeter's native unit for Constant Throughput Timer).
             1200/min = 20 rps, 6000/min = 100 rps, 12000/min = 200 rps. -->
        <perf.steady.threads>10</perf.steady.threads>
        <perf.steady.rampUp>15</perf.steady.rampUp>
        <perf.steady.duration>60</perf.steady.duration>
        <perf.steady.throughputPerMin>1200</perf.steady.throughputPerMin>

        <perf.peak.threads>50</perf.peak.threads>
        <perf.peak.rampUp>30</perf.peak.rampUp>
        <perf.peak.duration>300</perf.peak.duration>
        <perf.peak.throughputPerMin>6000</perf.peak.throughputPerMin>

        <perf.stress.threads>100</perf.stress.threads>
        <perf.stress.rampUp>30</perf.stress.rampUp>
        <perf.stress.duration>120</perf.stress.duration>
        <perf.stress.throughputPerMin>12000</perf.stress.throughputPerMin>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>com.lazerycode.jmeter</groupId>
            <artifactId>jmeter-maven-plugin</artifactId>
            <version>3.8.0</version>
            <executions>
              <execution>
                <id>configuration</id>
                <phase>verify</phase>
                <goals>
                  <goal>configure</goal>
                  <goal>jmeter</goal>
                  <goal>results</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <jmeterVersion>5.6.3</jmeterVersion>
              <testResultsTimestamp>false</testResultsTimestamp>
              <generateReports>true</generateReports>
              <ignoreResultFailures>false</ignoreResultFailures>
              <!-- Don't honor -DskipTests so this profile can run standalone via
                   `mvn -Pperf verify -DskipTests -DskipITs`. -->
              <skipTests>false</skipTests>
              <propertiesUser>
                <host>${perf.host}</host>
                <port>${perf.port}</port>
                <protocol>${perf.protocol}</protocol>
                <employeeId>${perf.employeeId}</employeeId>

                <steady.threads>${perf.steady.threads}</steady.threads>
                <steady.rampUp>${perf.steady.rampUp}</steady.rampUp>
                <steady.duration>${perf.steady.duration}</steady.duration>
                <steady.throughputPerMin>${perf.steady.throughputPerMin}</steady.throughputPerMin>

                <peak.threads>${perf.peak.threads}</peak.threads>
                <peak.rampUp>${perf.peak.rampUp}</peak.rampUp>
                <peak.duration>${perf.peak.duration}</peak.duration>
                <peak.throughputPerMin>${perf.peak.throughputPerMin}</peak.throughputPerMin>

                <stress.threads>${perf.stress.threads}</stress.threads>
                <stress.rampUp>${perf.stress.rampUp}</stress.rampUp>
                <stress.duration>${perf.stress.duration}</stress.duration>
                <stress.throughputPerMin>${perf.stress.throughputPerMin}</stress.throughputPerMin>
              </propertiesUser>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>
